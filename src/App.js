import { Button, Container } from "@mui/material";
const App = () => {
  return (
    <Container maxWidth="xl" sx={{ background: "#fff" }}>
      <Button>hello</Button>
    </Container>
  );
};

export default App;
